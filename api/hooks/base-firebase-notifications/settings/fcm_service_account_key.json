{
  "key": "FCM_SERVICE_ACCOUNT_KEY",
  "type": "json",
  "name": "FCM Service Account Key",
  "description": "Secret Firebase Admin SDK JSON file. Download it in Firebase Console: Project Settings -> Service accounts -> Generate new private key. Do not expose this key to the frontend.",
  "tooltip": "The Firebase Push page can upload the whole file at once. At minimum project_id, client_email and private_key are required.",
  "defaultValue": null,
  "isRequired": false,
  "module": "base-firebase-notifications",
  "jsonSchema": {
    "type": "object",
    "required": ["project_id", "private_key", "client_email"],
    "additionalProperties": true,
    "properties": {
      "type": { "type": "string" },
      "project_id": { "type": "string", "minLength": 1 },
      "private_key_id": { "type": "string" },
      "private_key": { "type": "string", "minLength": 1 },
      "client_email": { "type": "string", "minLength": 1 },
      "client_id": { "type": "string" },
      "auth_uri": { "type": "string" },
      "token_uri": { "type": "string" }
    }
  }
}
