JWT_SECRET=your_jwt_secret_here
# OPENHARNESS_API_KEY=
# OPENHARNESS_BROKER_TOKEN=

# Prometheus /metrics — the endpoint stays off until a token is set.
# Generate with: openssl rand -hex 32. Details: api/hooks/metrics/docs.md
# METRICS_TOKEN=
# STACK_NAME=